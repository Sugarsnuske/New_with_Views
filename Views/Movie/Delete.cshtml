@model New_with_Views.Models.Entities.Movie

<!-- Shared View hentes -->
    @{
        ViewBag.Title = "Delete Movie";
        ViewBag.Phrase = "Are you sure you want to delete this Movie?";

    }

<form asp-controler="Movie" asp-action="Delete" method="POST" class="form-horizontal">
    @Html.Partial("_MoviesDetails")
    <div class="form-group">
            <div class="col-md-offset-2 col-md-4">
                <button type="submit" class="btn btn-danger">Delete</button>
                <a class="btn btn-info" asp-controller="Movie" asp-action="Index">Back to List</a>
            </div>
        </div>
</form>
